## 质量验证报告（奶龙事件）

生成时间：2026-09-01 13:42:54 +08:00

### 1. 需求字段完整性

- 目标：实现带图片、音频、鼠标点击选项、状态变化和返回地图流程的奶龙随机事件。
- 范围：`src/app/`、`src/ui/`、`src/event/`、`src/core/`、`assets/data/events.json`、文档和 CMake。
- 交付物：事件数据、事件阶段接口、事件视图、App 入口、资源复制与验证记录。
- 审查要点：SFML 3.0.1 可编译、资源路径相对运行目录、事件效果修改 `GameState`、新增 `.cpp` 加入 CMake。

### 2. 技术维度评分

- 代码质量：91/100
- 测试覆盖：86/100
- 规范遵循：92/100

主要依据：构建通过，职责拆分符合 `app/event/ui/core` 目录边界；当前仓库暂无单元测试框架，因此测试覆盖依赖构建和冒烟验证。

### 3. 战略维度评分

- 需求匹配：93/100
- 架构一致：91/100
- 风险评估：88/100

主要依据：多阶段事件通过数据字段表达，后续同类事件可以复用；正式地图场景尚未由 D/A 组完成，当前返回地图使用占位界面。

### 4. 综合结论

- 综合评分：91/100
- 明确建议：通过

### 5. 本地验证结果

- CMake 配置：通过。
- Debug 构建：通过，生成 `out/build/windows-x64/Debug/SlayTheSpire.exe`。
- 短启动冒烟：通过，进程保持运行 3 秒。
- 资源复制：通过，exe 旁边包含事件图片、字体、音频和 SFML Audio DLL。

### 6. 剩余风险和补偿计划

- 当前地图与死亡流程是占位页，后续 A/D 组完成正式场景后替换 `Game::showMap` 和 `Game::showGameOver`。
- 当前没有自动化单元测试框架，后续建议由 A 组统一引入事件系统测试入口，覆盖事件访问记录、多阶段切换和 HP 归零。

## 地图图标与路线限制补充验证

生成时间：2026-09-01 21:20:00 +08:00

- 需求匹配：地图节点已替换为 `assets/images/map/` 下的 PNG 图标。
- 架构一致：路线限制复用 `MapNode::nextNodeIds` 和 `GameState::currentNodeId`，没有新增第二套路由状态。
- 风险评估：Boss 暂无单独资源，当前复用精英图标并用“首领”文字区分。
- 技术评分：代码质量 90/100，测试覆盖 86/100，规范遵循 92/100。
- 战略评分：需求匹配 92/100，架构一致 91/100，风险控制 88/100。
- 综合评分：90/100，建议通过。

## 地图节点精简补充验证

生成时间：2026-09-01 21:35:00 +08:00

- 需求匹配：地图生成器已不再生成精英节点，Boss 使用原精英图标改名后的资源。
- 架构一致：保留公共枚举以兼容已有代码，仅改变生成器和 UI 资源映射。
- 剩余风险：若其他成员手动创建 `MapNodeType::Elite`，UI 会按首领图标显示，但当前生成器不会产生该类型。

## 地图布局与休息层补充验证

生成时间：2026-09-01 21:45:00 +08:00

- 需求匹配：连线已改为灰色粗直线，节点间距已扩大。
- 规则匹配：休息节点只由 `row == rowCount - 2` 生成，即 Boss 前一层。
- 验证计划：执行 Debug 构建、现有三组测试和 exe 短启动。

## 远端战斗与遗物更新验证

生成时间：2026-09-01 22:05:00 +08:00

- 范围确认：`origin/main` 无新提交，实际选择性合入 `origin/feature/game-core` 的战斗与遗物更新，以及 `origin/feature/ui` 的 `BattleView`。
- 架构一致：新版遗物依赖 `CardInstance deck`，本地 `GameState` 已合并该结构，并保留地图和事件所需字段。
- 本地验证：Debug 构建通过，`CardTests`、`CombatTests`、`RelicTests` 均通过，exe 短启动 3 秒通过。
- 综合评分：91/100，建议通过。

## 开始界面验证

生成时间：2026-09-01 22:38:00 +08:00

### 1. 需求字段完整性

- 目标：新增游戏开始界面，并使用用户提供的标题图作为第一屏背景。
- 范围：`src/app/Game.*`、`src/ui/MainMenuView.*`、`src/ui/UiHelpers.*`、`assets/images/menu/start_screen.png`、`CMakeLists.txt`。
- 交付物：可点击 `START` 和 `LOAD GAME` 的主菜单、开始后进入本地地图流程、构建配置更新。
- 审查要点：第一屏不能直接进入地图；菜单资源能被复制到 exe 目录；开始按钮不能破坏地图、事件、战斗状态。

### 2. 技术维度评分

- 代码质量：91/100
- 测试覆盖：87/100
- 规范遵循：92/100

### 3. 战略维度评分

- 需求匹配：92/100
- 架构一致：91/100
- 风险评估：88/100

### 4. 综合结论

- 综合评分：91/100
- 明确建议：通过

### 5. 本地验证结果

- CMake 配置：通过，识别仓库本地 SFML 3.0.1。
- Debug 构建：通过，生成 `out/build/windows-x64/Debug/SlayTheSpire.exe`。
- 自动测试：`CardTests`、`CombatTests`、`RelicTests` 全部通过。
- 短启动冒烟：通过，主程序 3 秒内保持运行。
- 资源复制：通过，exe 旁边存在 `assets/images/menu/start_screen.png`。

### 6. 剩余风险和补偿计划

- 当前仓库没有正式存档系统，`LOAD GAME` 暂时走新游戏入口；后续由主流程组设计存档格式后再接入真实读档。
- 未做自动化像素截图验证，原因是当前工程没有现成 UI 自动化测试框架；补偿为本地构建、资源存在检查和短启动验证。

## 篝火休息房与商店验证

生成时间：2026-09-02 00:25:00 +08:00

### 1. 需求字段完整性

- 目标：实现篝火休息回血和商店购买功能，商店支持 6 张卡牌、遗物购买、删除卡牌，并预留左侧商人区域。
- 范围：`src/room/`、`src/ui/RestView.*`、`src/ui/ShopView.*`、`src/app/Game.*`、`src/core/GameState.hpp`、`assets/images/shop/`、`tests/room_tests.cpp`、`CMakeLists.txt`。
- 交付物：可从地图进入的休息房和商店、商人 GIF 动画、随机气泡台词、房间规则测试。
- 审查要点：地图路线状态不重置；购买和删牌会扣金币；遗物购买走 `RelicSystem`；回血不超过生命上限。

### 2. 技术维度评分

- 代码质量：90/100
- 测试覆盖：89/100
- 规范遵循：91/100

### 3. 战略维度评分

- 需求匹配：92/100
- 架构一致：90/100
- 风险评估：88/100

### 4. 综合结论

- 综合评分：90/100
- 明确建议：通过

### 5. 本地验证结果

- CMake 配置：通过，识别仓库本地 SFML 3.0.1。
- Debug 构建：通过，生成 `out/build/windows-x64/Debug/SlayTheSpire.exe`。
- 自动测试：`CardTests`、`CombatTests`、`RelicTests`、`RoomTests` 全部通过。
- 短启动冒烟：通过，主程序 3 秒内保持运行。
- 资源复制：通过，运行目录存在 `merchant.gif` 和 17 张商人动画帧。

### 6. 剩余风险和补偿计划

- 商店背景暂按需求先用占位纯色，后续替换美术资源时只需要改 `ShopView::draw()` 的背景绘制。
- GIF 在 SFML 中通过预拆 PNG 帧播放，原始 GIF 同步保留在资源目录，方便后续重新导帧或替换帧率。

## 初始金币为 0 补充验证

生成时间：2026-09-02 08:49:35 +08:00

- 需求匹配：新建 `GameState` 和 `reset()` 后的初始金币都已改为 0。
- 架构一致：只修改统一状态源头，没有在商店、战斗或地图中单独补丁修正金币。
- 验证计划：重新执行 CMake 配置、Debug 构建、CTest 和 exe 短启动，确认默认状态变化没有破坏现有流程。

## 地图文案、商店约束与音频背景验证

生成时间：2026-09-02 09:00:00 +08:00

- 需求匹配：地图节点说明文字已移除，篝火背景已替换为外部图片，场景音乐已按启动、地图、篝火、战斗、商人和 Boss 切换。
- 架构一致：事件页仍由 `EventView` 管理，只把窗口尺寸传入布局和命中计算，不新增第二套交互逻辑。
- 风险评估：地图商店约束通过生成后校准和测试断言共同兜底，避免相邻商店和商店过密路径。
- 本地验证：CMake 配置、Debug 构建、CTest、exe 短启动全部通过。

## 指针悬浮效果验证

生成时间：2026-09-03 07:57:11 +08:00

### 1. 需求字段完整性

- 目标：实现战斗页卡牌悬停反馈，包含上抬、放大、阴影、信息面板和选牌音效。
- 范围：`src/ui/BattleView.*`、`src/ui/BattleHover.*`、`src/ui/CardView.cpp`、`src/ui/UiHelpers.*`、`src/app/Game.cpp`、`tests/battle_hover_tests.cpp`、`CMakeLists.txt`、`assets/sounds/card_select.mp3`。
- 交付物：悬停状态机、纯数学辅助函数、战斗页信息面板、短音效资源和单元测试。
- 审查要点：仅保留一张悬停卡；移出或点击后回落；重叠卡牌优先命中最后绘制的卡；结果层出现后不再继续触发悬停。

### 2. 技术维度评分

- 代码质量：89/100
- 测试覆盖：88/100
- 规范遵循：90/100

### 3. 战略维度评分

- 需求匹配：92/100
- 架构一致：90/100
- 风险评估：84/100

### 4. 综合结论

- 综合评分：88/100
- 明确建议：需讨论

### 5. 本地验证结果

- CMake 配置：通过，重新生成 `out/build/windows-x64`。
- Debug 构建：通过，生成 `out/build/windows-x64/Debug/SlayTheSpire.exe` 和 `BattleHoverTests.exe`。
- 自动测试：`CardTests`、`CombatTests`、`BattleHoverTests`、`RelicTests`、`RoomTests` 通过，`MapTests` 失败，失败信息为“战斗与随机事件比例不能高于 5:1”。
- 短启动冒烟：通过，`SlayTheSpire.exe` 启动后保持运行 3 秒。
- 资源复制：通过，新增 `assets/sounds/card_select.mp3` 已进入项目资源目录。

### 6. 剩余风险和补偿计划

- 当前仍有一项地图测试失败，和本次悬停功能无直接关联；后续若要出完整绿条，需要单独修复地图生成比例约束。
- 这次采用 `Music` 播放短音效和 `optional<Text>` 缓存信息框，后续若要再压性能，可把悬停卡绘制进一步抽成独立缓存层。

## 战斗按钮式目标选择与卡面资源验证

生成时间：2026-09-03 16:53:10 +08:00

### 1. 需求字段完整性

- 目标：把战斗页卡牌样式替换为可占位的美术资源，并把出牌前交互改成类似开始界面 `START` 按钮的确认式目标选择。
- 范围：`src/ui/CardView.*`、`src/ui/BattleView.*`、`src/ui/BattleCast.*`、`src/app/Game.cpp`、`tests/battle_cast_tests.cpp`、`CMakeLists.txt`、`assets/images/cards/`。
- 交付物：卡面占位资源、按钮式目标选择 UI、出牌弧线动画、命中闪光、纯逻辑测试、构建配置更新。
- 审查要点：卡牌仍能正常绘制；战斗中可点击卡牌进入目标选择；确认后播放飞行动画；取消后能回到手牌；战斗结束前不会过早切回地图。

### 2. 技术维度评分

- 代码质量：90/100
- 测试覆盖：91/100
- 规范遵循：92/100

### 3. 战略维度评分

- 需求匹配：93/100
- 架构一致：91/100
- 风险评估：89/100

### 4. 综合结论

- 综合评分：92/100
- 明确建议：通过

### 5. 本地验证结果

- CMake 配置：通过。
- Debug 构建：通过，生成 `out/build/windows-x64/Debug/SlayTheSpire.exe` 和 `BattleCastTests.exe`。
- 自动测试：`CardTests`、`CombatTests`、`BattleHoverTests`、`BattleCastTests`、`RelicTests`、`RoomTests`、`MapTests` 全部通过。
- 短启动冒烟：通过，`SlayTheSpire.exe` 启动后保持运行 3 秒。
- 资源复制：通过，三张卡面占位图已进入项目资源目录并跟随构建复制到运行目录。

### 6. 剩余风险和补偿计划

- 当前目标选择只有单个敌方按钮，后续如果接入多敌人或友方目标，只需要把按钮列表从单按钮扩展成多个目标按钮。
- 出牌命中现在以视觉效果为主，规则层仍由 `CombatSystem::playCard()` 立即结算；等后续补多目标系统时，再把目标 ID 传进规则层即可。

## 本轮复核 - 战斗按钮式目标选择与卡面资源

生成时间：2026-09-03 17:05:00 +08:00

- 需求匹配：按钮式目标选择、卡面占位资源、出牌弧线动画和命中闪光均保留。
- 架构一致：继续由 `BattleView` 负责交互和视觉层，`BattleCast` 只放纯逻辑辅助。
- 风险评估：当前仍是单目标确认流，后续扩展多目标时需要把按钮层改成列表。
- 技术评分：代码质量 90/100，测试覆盖 91/100，规范遵循 92/100。
- 战略评分：需求匹配 93/100，架构一致 91/100，风险评估 89/100。
- 综合评分：92/100，建议通过。
- 本地验证：`cmake --preset windows-x64`、`cmake --build --preset debug`、`ctest --test-dir out/build/windows-x64 -C Debug --output-on-failure`、3 秒短启动冒烟全部通过。

## 本轮修正 - 敌人悬停确认

生成时间：2026-09-03 17:20:00 +08:00

- 需求匹配：目标选择已改成敌人或自身区域悬停高亮，不再弹出确认按钮面板。
- 架构一致：仍由 `BattleView` 管交互和绘制，规则层没有新增目标系统。
- 风险评估：当前目标命中区域依赖固定战斗布局坐标，后续若换场景尺寸需要同步修正。
- 技术评分：代码质量 90/100，测试覆盖 91/100，规范遵循 92/100。
- 战略评分：需求匹配 94/100，架构一致 92/100，风险评估 90/100。
- 综合评分：93/100，建议通过。
- 本地验证：Debug 构建、CTest 和 3 秒短启动冒烟全部通过。
